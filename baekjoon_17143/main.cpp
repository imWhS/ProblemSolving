/*
 * 낚시왕
 * input:
 * - 격자판의 크기 R, C와 상어의 수 M
 * - 상어 위치 r, c와 속력 s, 이동 방향 d, 크기 z (단, d가 1부터 4까지 위, 아래, 오른쪽, 왼쪽 순)
 * - 단, 두 상어가 같은 크기를 갖는 경우는 없고 하나의 칸에 둘 이상의 상어가 있는 경우는 없다.
 * - 한 칸(r, c)에 두 마리 이상의 상어가 있는 경우, 가장 크기가 큰 상어가 나머지 상어를 모두 잡아먹는다.
 * output: 낚시왕이 잡은 상어 크기의 합
 */

#include <iostream>

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}